
# Simulate data with and without normality --------------------------------
gen_sim_norm <- function(n, k, mean.v=rep(0,k), sd.v=rep(1,k), normality=TRUE){
  print(mean.v)
  print(sd.v)
}

gen_sim_norm(100,3)

